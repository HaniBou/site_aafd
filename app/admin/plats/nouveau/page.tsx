import PlatFormClient from '@/components/admin/PlatFormClient';

export const metadata = { title: 'Nouveau plat — Admin AAFD' };

export default function NouveauPlatPage() {
  return <PlatFormClient />;
}
